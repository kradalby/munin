# Tooling
brew 'swift'
brew 'swiftlint'
brew 'swift-format'

# Dependencies
# vips: swift-vips' Cvips target is pkgConfig: "vips" -- without it the build
# fails at configuration time, not at link time.
brew 'vips'
brew 'libexif'
brew 'libiptcdata'
